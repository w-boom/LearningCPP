// std::size_t
// std::size_t is the unsigned integer type of the result of the sizeof operator
#include <iostream>

int main()
{
    std::size_t a = 3;
    std::size_t b = 5;
    std::cout << a - b << std::endl;
    
    return 0;
}